import './_styles.scss';
import React, { FunctionComponent } from 'react';

interface Props {
  title: string;
}
const Header: FunctionComponent<Props> = () => (
  <header />
);

export default Header;
