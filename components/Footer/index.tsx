import './_styles.scss';
import React, { FunctionComponent } from 'react';

interface Props {
  title: string;
}
const Footer: FunctionComponent<Props> = () => (
  <footer />
);

export default Footer;
